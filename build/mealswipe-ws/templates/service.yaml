apiVersion: v1
kind: Service
metadata:
  name: {{ include "mealswipe-ws.fullname" . }}
  labels:
    {{- include "mealswipe-ws.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "mealswipe-ws.selectorLabels" . | nindent 4 }}
