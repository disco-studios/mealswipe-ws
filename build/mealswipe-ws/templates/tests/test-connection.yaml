apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mealswipe-ws.fullname" . }}-test-connection"
  labels:
    {{- include "mealswipe-ws.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mealswipe-ws.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
